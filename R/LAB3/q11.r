int_list <- 1:5

# Create second list of month names
month_list <- c("January", "February", "March", "April", "May")

# Merge the two lists
merged_list <- c(int_list, month_list)

# Display the merged list
print(merged_list)